# 슬라임게임 작업 기록

파일 위치: `01_포토폴리오/slimegame.html` (원본: `02_게임 개발/슬라임게임/slimegame.html`, 두 파일 동일하게 유지)

## 2026-07-03 — 멀티플레이 원격 접속 안 되는 문제 수정

### 증상
- 같은 네트워크(같은 와이파이)에서는 멀티 연결이 되지만, 다른 네트워크에 있는 친구와는 "연결 중..."만 뜨고 접속 불가.

### 원인
- PeerJS를 기본 설정(`new Peer()`)으로 사용 → STUN 서버만 사용하고 TURN 릴레이가 없음.
- 한국 통신사 NAT·공유기 이중 NAT 환경에서는 STUN만으로 P2P 직접 연결이 자주 실패함.
- 실패해도 타임아웃 처리가 없어 무한 대기 상태가 됨.

### 수정 내용
1. **무료 공개 TURN 서버 추가** — `PEER_CONFIG`에 Google STUN 2개 + Open Relay(metered.ca) TURN 3개(80, 443, 443/tcp) 등록. 직접 연결이 막히면 릴레이로 우회.
2. **16초 연결 타임아웃** — 참가 측에서 16초 안에 연결이 안 되면 "연결 시간 초과" 메시지와 함께 **다시 연결 시도** 버튼 표시 (기존: 무한 대기).
3. **단계별 상태 메시지** — 참가: `1/3 연결 서버 접속 → 2/3 상대방에게 연결 요청 → 연결 완료`. 호스트: `코드 생성 완료 → 상대방 발견 → 연결 완료`. `peer-unavailable` 오류는 "방을 찾을 수 없습니다"로 안내.

### 한계 / 참고
- Open Relay는 무료 공개 서비스라 가끔 느리거나 다운될 수 있음. 그 경우 [metered.ca](https://www.metered.ca/tools/openrelay/)에서 무료 API 키를 발급받거나 다른 TURN 서비스로 교체 필요.
- TURN 경유 시 지연이 늘 수 있으나 이 게임은 80ms 간격 상태 동기화라 체감 영향은 적음.
